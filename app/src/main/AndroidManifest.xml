<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.com.expressapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity android:name=".login.view.LoginActivity">
        </activity>
        <activity android:name=".basispage.view.BasisPageActivity">
        </activity>
        <activity android:name=".welcome.view.WelcomeActivity"></activity>
        <activity android:name=".JJSearch.TestActivity">

        </activity>
        <activity android:name=".welcome.presenter.JudgeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>